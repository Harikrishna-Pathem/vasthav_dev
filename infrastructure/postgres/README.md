# PostgreSQL

Local PostgreSQL is defined in the root `docker-compose.yml`. Persistent data is held in the named `postgres_data` volume. Remove it only when intentionally resetting local development data.
