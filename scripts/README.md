# Scripts

Repository scripts are declared in workspace `package.json` files so CI and developers run the same commands. Add operational scripts here only when they cannot be expressed safely as package scripts.
